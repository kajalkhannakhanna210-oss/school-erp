"use client";
import { useState, type FormEvent } from "react"; import { createClient } from "@/lib/supabase/client"; import { Button, Input, Label } from "@/components/ui";
export default function AlumniPage(){const [sent,setSent]=useState(false),[error,setError]=useState("");const [form,setForm]=useState({full_name:"",email:"",phone:"",graduation_year:"",occupation:"",message:""});async function submit(e:FormEvent){e.preventDefault();const {error}=await createClient().from("alumni_registrations").insert({...form,graduation_year:Number(form.graduation_year),phone:form.phone||null,occupation:form.occupation||null,message:form.message||null});if(error)setError(error.message);else setSent(true)}return <div className="mx-auto max-w-3xl px-6 py-16"><p className="font-mono text-xs uppercase tracking-[.2em] text-gold-600">Alumni community</p><h1 className="mt-4 font-display text-4xl text-ink-700">Alumni registration</h1>{sent?<p className="mt-8 rounded-xl bg-success/10 p-6 text-success">Thank you for registering. We’ll keep you connected with school news.</p>:<form onSubmit={submit} className="mt-8 grid gap-4 rounded-xl border border-ink-100 bg-white p-6 sm:grid-cols-2"><div><Label>Full name</Label><Input required value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></div><div><Label>Email</Label><Input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div><Label>Phone</Label><Input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><div><Label>Graduation year</Label><Input required type="number" min="1950" max="2100" value={form.graduation_year} onChange={e=>setForm({...form,graduation_year:e.target.value})}/></div><div className="sm:col-span-2"><Label>Current occupation</Label><Input value={form.occupation} onChange={e=>setForm({...form,occupation:e.target.value})}/></div><div className="sm:col-span-2"><Label>Message</Label><textarea className="mt-1 w-full rounded-lg border border-ink-100 p-3" value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></div>{error&&<p className="text-danger sm:col-span-2">{error}</p>}<Button>Register</Button></form>}</div>}

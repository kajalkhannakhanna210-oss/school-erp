import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { requirePageAccess } from "@/lib/require-role";
export async function PATCH(req: Request, { params }: { params: { id: string } }) { try { await requirePageAccess("school_master"); } catch { return NextResponse.json({ error: "Forbidden" }, { status: 403 }); } const body = await req.json().catch(() => ({})); const organization_id = String(body.organization_id ?? ""); const name = String(body.name ?? "").trim(); const code = String(body.code ?? "").trim().toUpperCase(); const slug = String(body.slug ?? "").trim().toLowerCase(); if (!params.id || !organization_id || !name || !code || !slug) return NextResponse.json({ error: "Organization, name, code, and slug are required." }, { status: 400 }); const supabase = await createClient(); const { data: org } = await supabase.from("organizations").select("id").eq("id", organization_id).maybeSingle(); if (!org) return NextResponse.json({ error: "Selected organization does not exist." }, { status: 400 }); const { data, error } = await supabase.from("schools").update({ organization_id, name, code, slug, is_active: body.is_active !== false, updated_at: new Date().toISOString() }).eq("id", params.id).select().single(); if (error) return NextResponse.json({ error: error.code === "23505" ? "School code or slug already exists." : error.message }, { status: error.code === "23505" ? 409 : 500 }); return NextResponse.json({ school: data }); }
